<h1><%= t('events.header.all') %></h1>
<%= link_to t('events.button.new'), new_event_path %>

<table>
  <tr>
    <th><%= t('events.index.title') %></th>
    <th><%= t('events.index.description') %></th>
    <th><%= t('events.index.location') %></th>
    <th><%= t('events.index.start_time') %></th>
    <th><%= t('events.index.end_time') %></th>
    <th><%= t('events.index.organizer_email') %></th>
    <th><%= t('events.index.organizer_telegram') %></th>
    <th><%= t('events.index.link') %></th>
    <th></th>
  </tr>

  <% @events.each do |event| %>
    <tr>
      <td><%= link_to event.title, event_path(event) %></td>
      <td><%= event.description %></td>
      <td><%= event.location %></td>
      <td><%= event.start_time %></td>
      <td><%= event.end_time %></td>
      <td><%= event.organizer_email %></td>
      <td><%= event.organizer_telegram %></td>
      <td><%= event.link %></td>
      <td><%= link_to t('events.button.show'), event_path(event) %></td>
      <td><%= link_to t('events.button.edit'), edit_event_path(event) %></td>
      <td><%= button_to t('events.button.delete'), event_path(event),
              method: :delete%></td>
    </tr>
  <% end %>
</table>
<%= paginate @events %>