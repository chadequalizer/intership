<h1>All events:</h1>
<%= link_to 'New event', new_event_path %>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Location</th>
    <th>Start time</th>
    <th>End time</th>
    <th>Organizer email</th>
    <th>Organizer telegram</th>
    <th>Link</th>
    <th></th>
  </tr>

  <% @events.each do |event| %>
    <tr>
      <td><%= event.title %></td>
      <td><%= event.description %></td>
      <td><%= event.location %></td>
      <td><%= event.starttime %></td>
      <td><%= event.endtime %></td>
      <td><%= event.organizeremail %></td>
      <td><%= event.organizertelegram %></td>
      <td><%= event.link %></td>
      <td><%= link_to 'Show', event_path(event) %></td>
    </tr>
  <% end %>
</table>