language: ruby
services: 
  - postgresql
before_script:
  - psql -c 'create database test_psql;' -U postgres
addons:
  postgresql: "12.2"
  apt:
    packages:
      - postgresql-12.2
      - postgresql-client-12.2
env:
  global:
        - PGVER=12
        - PGPORT=5433